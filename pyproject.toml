[project]
name = "smart-contractor-matching"
version = "0.1.0"
description = "Deterministic contractor matching baseline for a hackathon prototype."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["app"]
